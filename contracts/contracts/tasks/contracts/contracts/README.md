## Deploy & proof in 2 min (phone)
1. `npm i` (once)
2. Copy `.env.example` → `.env` and add your Base-Sepolia private key
3. `npx hardhat deploy --network baseSepolia`
4. Screenshot the Basescan link that prints
5. Drop the screenshot into `/contracts/out/` and link it in root README
